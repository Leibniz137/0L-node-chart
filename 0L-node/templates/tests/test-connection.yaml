apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "0L-node.fullname" . }}-test-connection"
  labels:
    {{- include "0L-node.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "0L-node.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
